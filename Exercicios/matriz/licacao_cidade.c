/* Henrique Noronha Facioli
 * RA : 157986
 * Suponha que a matriz seguinte representa ligacoes entre cidades, e que, se uma posicao (i,j) possui o valor 1, entao ha uma ligacao da cidade i para a j.
 * Tendo essas informa ̧coes, faca um programa que seja capaz de:
 * Listar as cidades com entrada e sem saıda;
 * Listar as cidades com saıda e sem entrada;
 * Listar cidades isoladas;
 * Dizer, saindo de cada cidade, quais as que podem ser alcanc ̧adas diretamente.
 */

#include <stdio.h>

#define TAM 2

int main(){
    int ligacao[TAM][TAM];
    int entrada = 0, saida = 0;
    
    /* Entrada da matriz que contem as ligacoes*/
    for(i = 0; i < tamanho; i++){
        for(j = 0; j < tamanho; j++){
            scanf("%d", &matriz_1[i][j]);
        }
    }
    
    
    /* Imprime as cidades que tem entrada mas nao saida*/
    for(i = 0; i < tamanho; i++){
        saida = 0 
    }
}